export function speak(text:string){try{if(!('speechSynthesis'in window))return; window.speechSynthesis.cancel(); const u=new SpeechSynthesisUtterance(text); u.rate=.78; u.pitch=1; u.volume=1; window.speechSynthesis.speak(u)}catch{}}
export function tone(kind:'scan'|'success'|'failure'){try{const C=window.AudioContext||((window as unknown as {webkitAudioContext: typeof AudioContext}).webkitAudioContext); const ctx=new C(); const osc=ctx.createOscillator(); const gain=ctx.createGain(); osc.connect(gain); gain.connect(ctx.destination); osc.frequency.value=kind==='success'?660:kind==='failure'?180:440; gain.gain.value=.15; osc.start(); setTimeout(()=>{osc.stop(); void ctx.close()},kind==='scan'?120:260)}catch{}}
